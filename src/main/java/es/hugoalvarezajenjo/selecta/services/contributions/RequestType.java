package es.hugoalvarezajenjo.selecta.services.contributions;

public enum RequestType {
    ACCESS,    // Student requests to become a contributor
    RESOURCE   // Student proposes a specific resource
}
